{
  "name": "sealdice-affection-renderer",
  "version": "1.15.1",
  "private": true,
  "description": "Image renderer for the SealDice affection minigame collection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test ../tests/renderer.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.80"
  }
}
